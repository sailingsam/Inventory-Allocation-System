[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra --strict-markers
markers =
    postgres: test requires a real PostgreSQL backend (row-level locking / true concurrency)
